package main

// please use hard code version info.
// because build a executable file before determine a tag.
// this above will be almost resolved if stop use a this version info on useragent.
const version = "0.4.2"
